# 📱 健身打卡 · 手机部署傻瓜教程

> ✅ **这份已升级，请用新版：同目录下的 `手机使用与数据不丢失指南.html`**（双击打开，带步骤打勾和网址生成器）。
> 新版多了「云同步」部分——打卡后自动备份到云端，换手机/清缓存数据也不丢。
> 下面是旧版存档，只讲部署，仍然有效。

**目标**：让你在手机上像用 App 一样用这个打卡小程序（有桌面图标、全屏、断网也能用）。

**先了解一个关键点**：打卡数据保存在「浏览器」里，不联网、不注册。但换一个网址/入口打开，数据是分开的——所以换入口前记得在「统计」页【导出数据备份】，到新入口【导入数据备份】。

---

## 方式一：GitHub Pages（推荐 ✅ 一次部署，永久可用）

不需要会写代码，全部在网页上点按钮完成。

### 第一步：注册 GitHub（有账号就跳过）

1. 电脑浏览器打开 https://github.com
2. 点右上角 **Sign up** → 输入邮箱 → 设置密码 → 用户名随便起（英文/数字，比如 `qiandajie`）→ 邮箱验证码激活
3. 登录后看到个人首页即可

### 第二步：新建仓库

1. 点右上角 **＋** 号 → **New repository**
2. `Repository name` 填：`fitness`（名字随意，全英文）
3. 选 **Public**（免费方案必须是公开的，放心，里面只有你的打卡代码，没有你的打卡数据）
4. 勾选 **Add a README file**
5. 点绿色 **Create repository** 按钮

### 第三步：上传本项目文件

1. 进入刚建的仓库页面，点 **Add file** → **Upload files**
2. 在电脑上打开 `D:\健身打卡` 文件夹，**把里面所有文件和文件夹全选**（index.html、css、js、icons、manifest.json、sw.js），直接拖进网页中间的方框
   - ⚠️ 不要拖 `健身打卡` 这个文件夹本身，要拖它里面的内容
   - ⚠️ 不要拖 README.md 和 手机部署傻瓜教程.md（可拖可不拖，没影响）
3. 等文件列表全部上传完（显示绿色 ✓），点绿色 **Commit changes** 按钮

### 第四步：开启 Pages（关键一步）

1. 仓库页面顶部点 **Settings**（设置）
2. 左侧菜单点 **Pages**（在 Code and automation 分组下）
3. 中间 **Branch** 下拉框：选 **main**，右边选 **/ (root)**，点 **Save**
4. 等待 1~2 分钟，刷新页面，顶部会显示：
   `Your site is live at https://你的用户名.github.io/fitness/`
5. 点这个链接，能看到打卡页面就成功了 ✅

> 以后改了代码，重复「第三步」重新上传即可，手机端下次打开自动更新。

---

## 方式二：Cloudflare Pages（拖拽上传，比方式一还简单）

1. 浏览器打开 https://dash.cloudflare.com → 注册登录（免费）
2. 登录后左侧点 **Workers & Pages** → **Pages** → 点 **Upload assets**（上传资源）
3. 输入项目名 `fitness` → 点 **Create project**
4. 把 `D:\健身打卡` 文件夹**整个拖进**网页中间的方框（这个服务支持拖文件夹）
5. 等上传完，点 **Deploy site** → 完成后点 **Visit site** 查看效果
6. 点 **View project** → **Domain** 里能看到你的网址（形如 `fitness-xxxx.pages.dev`）

> 以后改了代码：重新进项目 → **Create new deployment** → 重新拖文件夹即可。

---

## 方式三：同 WiFi 局域网（不联网就能用，最快体验）

适合「电脑和手机在同一个 WiFi 下、临时用一下」的场景。电脑要一直开着。

1. 电脑上双击 `D:\健身打卡` 里的 **`手机访问-局域网.bat`**
2. 黑窗口会显示电脑的 IP 地址（如 `192.168.2.17`）
3. 手机连**同一个 WiFi**，浏览器打开 `http://192.168.2.17:8630`
4. 用完关掉黑窗口即可

> 注意：局域网方式没有离线缓存（不算真正的 PWA），且换 IP/换网络后入口变了，数据记得导出导入。想长期用建议方式一或二。

---

## 部署好后：把网页变成"App"

### iPhone（Safari）

1. 用 **Safari** 打开你的网址
2. 点底部中间的 **分享按钮**（方框带↑箭头）
3. 往下滑，点 **添加到主屏幕** → 右上角 **添加**
4. 桌面出现图标，点开就是全屏 App

### Android（Chrome 或 Edge）

1. 用 **Chrome/Edge** 打开网址
2. 点右上角 **⋮ 菜单** → **安装应用**（或"添加到主屏幕"）
3. 桌面出现图标，点开即全屏

---

## 数据迁移（换入口 / 换手机时）

1. 旧入口：「统计」页 →【导出数据备份】→ 得到一个 `.json` 文件，存到手机
2. 新入口：「统计」页 →【导入数据备份】→ 选择那个文件 → 确认覆盖
3. 完成，动作库和全部打卡记录都回来了

## 常见问题

- **图标是紫色、里面是绿色？** 「统计」页有「主题颜色」，点圆点随时切换，深夜黑也有。
- **iOS 数据丢失？** 没添加到主屏幕的网页，Safari 长期不访问会清理数据。添加到主屏幕后长期保留，也别忘了定期导出备份。
- **GitHub Pages 打不开？** 刚保存要等 1~2 分钟；还不行就检查 Settings → Pages 里 Branch 是否选了 main。
